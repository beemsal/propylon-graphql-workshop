{
  "name": "graphql-legislature",
  "version": "1.0.0",
  "description": "GraphQL server for US state legislature data",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "vercel-build": "echo 'No build step required'"
  },
  "dependencies": {
    "apollo-server-express": "^3.12.0",
    "apollo-server-core": "^3.12.0",
    "express": "^4.18.2",
    "graphql": "^16.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}